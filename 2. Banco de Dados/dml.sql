use superheroiDB;

insert into tb_super_heroi ( nm_super_heroi, ds_super_poder, bt_voa )
					values ( 'Superman', 'voar, super soco, força bruta', true );
                    
                    
select * 
  from tb_super_heroi
  where nm_super_heroi = 1;